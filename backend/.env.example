# Pixiv Refreh Token
# 获取方式: python -c "from pixivpy3 import AppPixivAPI; a=AppPixivAPI(); a.login('用户名', '密码'); print(a.refresh_token)"
PIXIV_REFRESH_TOKEN=your_refresh_token_here

# 服务配置
HOST=0.0.0.0
PORT=8000

# 限流
RATE_LIMIT_PER_MIN=30
